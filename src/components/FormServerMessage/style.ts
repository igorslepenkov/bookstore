import styled from "styled-components";
import { Color, fonts } from "../../ui";

export const StyledFormServerMessage = styled.p`
  padding: 20px;
  ${fonts.bodyRegular};
  color: ${Color.Black};
  background-color: ${Color.PinkLight};
`;
